<script type="text/template" id="contact-template">
    <form role="form" action="/submitContact" method="post" id="contactForm">
        <div class="form-group">
            <label for="firstname">名 *</label>
            <input type="text" class="form-control" name="firstname" value="{{=contact? contact.get('firstname'):''}}" required  placeholder="prénom">
        </div>
        <div class="form-group">
            <label for="familyname">姓 </label>
            <input type="text" class="form-control" name="familyname" value="{{=contact? contact.get('familyname'):''}}"  placeholder="nom">
        </div>
        <div class="form-group">
            <label for="phonenumber">电话</label>
            <input type="text" class="form-control" name="phonenumber" value="{{=contact? contact.get('phonenumber'):''}}" placeholder="06 xx xx xx xx xx">
        </div>
        <div class="form-group">
            <label for="email">邮件</label>
            <input type="text" class="form-control" name="email" value="{{=contact? contact.get('email'):''}}"  placeholder="xxx@yyy.com ">
        </div>
        <div class="form-group">
            <label for="notes">备忘</label>
            <input type="text" class="form-control" name="notes" value="{{=contact? contact.get('notes'):''}}" >
        </div>

        <div class="form-group">
            <a href="/a/restos" class="btn btn-default">返回</a> &nbsp; &nbsp;
            <input type="submit" id="submitContact" class="btn btn-primary" value="保存">
        </div>

    </form>
</script>